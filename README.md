# pulltext-engine

Motor de extração de layout e texto para a API REST do Figma, com inferência espacial e conversão para Markdown.

## Estrutura

- [packages](packages) — pacote publicado como `@pulltext/engine`

## Desenvolvimento

```bash
cd packages
npm install
npm run build
```
